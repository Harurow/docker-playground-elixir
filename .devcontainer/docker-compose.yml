version: '3'
services:
  playground:
    container_name: 'elixir'
    hostname: 'elixir'
    build: .
    restart: always
    working_dir: '/opt'
    tty: true
    volumes:
      - type: bind
        source: ../opt
        target: /opt
